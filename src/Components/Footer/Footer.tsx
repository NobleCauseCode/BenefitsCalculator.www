import { Box } from '@mui/material';
export const Footer = () => {
  return (
    <Box sx={{ backgroundColor: (theme) => theme.palette.primary.main, color: '#fff', padding: 4 }}>
      Interview Submission - Jeff Noble
    </Box>
  );
};
